<html>
  <head>
    <meta charset='utf-8' />
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        HauTrank2
      {{/if}}

    </title>
  </head>
  <body>
    {{>header}}
    {{{body}}}
    {{>footer}}
  </body>
</html>